server:
  port: ${SERVER_PORT:8010}
spring:
  application:
    name: org
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:111.229.218.230}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:111.229.218.230}:${REGISTER_PORT:8848}
        file-extension: yml
