server:
  port: 8001
spring:
  application:
    name: gateway
  kafka:
    bootstrap-servers: "10.0.4.138:8088"
    consumer:
      group-id: "gatewayConfig"
  security:
    oauth2:
      jwt:
        signingKey: runyuanj123
  profiles:
    include: core, log, gateway, gateway-cli

gate:
  ignore:
    authentication:
      startWith: /oauth,/open

logging:
  file:
    name: gateway.log