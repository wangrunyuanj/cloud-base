server:
  port: 8001
spring:
  application:
    name: gateway
  security:
    oauth2:
      jwt:
        signingKey: runyuanj123
  profiles:
    include: core, log, message, gateway, gateway-cli

gate:
  ignore:
    authentication:
      startWith: /oauth,/open

logging:
  file:
    name: logs/gateway.log